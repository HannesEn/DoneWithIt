import React from "react";
import { View, StyleSheet } from "react-native";

function Card(props) {
  return <View></View>;
}

const styles = StyleSheet.create({
  card: {
    borderRadius: 15,
  },
});

export default Card;
